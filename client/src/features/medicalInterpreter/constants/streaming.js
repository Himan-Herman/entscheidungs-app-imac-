/** Client streaming limits — align with server `interpreterStreamEnv.js` (Phase 5.3). */

/** Max streaming session length before auto-stop (ms). */
export const INTERPRETER_STREAM_MAX_DURATION_MS = 120_000;
